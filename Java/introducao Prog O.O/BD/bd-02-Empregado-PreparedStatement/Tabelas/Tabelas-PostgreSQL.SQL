DROP TABLE EMPREGADOS;
-- DROP SEQUENCE EMPREGADOS_SEQ;

CREATE SEQUENCE EMPREGADOS_SEQ;

CREATE TABLE EMPREGADOS
(NUMERO       NUMERIC(5,0)
   CONSTRAINT EMPREGADOS_NUMERO_PK
   PRIMARY KEY,
 NOME         VARCHAR(50),
 SALARIO      NUMERIC(9,2));

INSERT INTO EMPREGADOS
VALUES (NEXTVAL('EMPREGADOS_SEQ'), 'JULIO CESAR', 2500.00);

INSERT INTO EMPREGADOS
VALUES (NEXTVAL('EMPREGADOS_SEQ'), 'JULIANA SILVA', 1800.00);

INSERT INTO EMPREGADOS
VALUES (NEXTVAL('EMPREGADOS_SEQ'), 'SERGIO ARANTES', 3800.00);

SELECT * FROM EMPREGADOS;
